import { TextExpanderDirective } from './text-expander.directive';

describe('TextExpanderDirective', () => {
  it('should create an instance', () => {
    const directive = new TextExpanderDirective();
    expect(directive).toBeTruthy();
  });
});
